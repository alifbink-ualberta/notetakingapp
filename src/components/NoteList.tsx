import { Note } from "./types.ts"

type NoteListProps = {
  notes: Note[]
  onSelect: (id: string) => void
}

function NoteList() {
    return (
        <div>
            
        </div>
    )
}


export default NoteList